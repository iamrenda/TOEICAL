import { Text, View } from "react-native";
import React from "react";

const Refresh = () => {
    return (
        <View>
            <Text>Refresh</Text>
        </View>
    );
};

export default Refresh;
